#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "proba.h"

float ecartarrivee (int lbd)
{
    float U=0;
    srand(time(NULL));
    U=(float)rand();//(float)RAND_MAX;
    printf("%d\n", RAND_MAX);
    float T=0;
    return T;
}
